import { Component } from "react"

export default class App extends Component {
  render() { 
  <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101'
      }}
    >
      
    </div>
  };
};
